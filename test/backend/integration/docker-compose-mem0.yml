version: '3.8'

services:
  mem0:
    image: mem0/mem0:latest
    ports:
      - "8765:8765"
    environment:
      - MEM0_STORAGE=local
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8765/health"]
      interval: 5s
      timeout: 3s
      retries: 10
