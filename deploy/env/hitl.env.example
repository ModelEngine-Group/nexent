# Optional durable human interaction. Merge into the runtime service environment.
# Keep disabled until the SQL migration and a shared persistent Fernet key are installed.
HITL_ENABLED=false
HITL_ACCEPT_NEW_RUNS=true
HITL_ENCRYPTION_KEY=
HITL_WAIT_SECONDS=86400
HITL_MAX_CONCURRENCY=2
