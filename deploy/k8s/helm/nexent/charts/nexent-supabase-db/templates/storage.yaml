{{- if or (eq .Values.global.deploymentVersion "full") (index .Values.global.deploymentComponents "supabase") }}
{{- $globalShared := default dict .Values.global.sharedStorage }}
{{- $storageClassName := default (default "" $globalShared.storageClassName) .Values.persistence.storageClassName }}
{{- $mode := default "local" .Values.persistence.mode }}
{{- if eq $mode "local" }}
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nexent-supabase-db-pv
  labels:
    type: hostpath
    app: nexent-supabase-db
  annotations:
    "helm.sh/hook-weight": "-2"
spec:
  storageClassName: {{ $storageClassName | quote }}
  capacity:
    storage: {{ .Values.storage.size }}
  accessModes:
{{ toYaml .Values.persistence.accessModes | indent 4 }}
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: {{ .Values.persistence.localPath | quote }}
    type: DirectoryOrCreate
{{- end }}
{{- if ne $mode "existing" }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexent-supabase-db
  namespace: {{ .Values.global.namespace }}
  annotations:
    "helm.sh/hook-weight": "-2"
spec:
  accessModes:
{{ toYaml .Values.persistence.accessModes | indent 4 }}
  resources:
    requests:
      storage: {{ .Values.storage.size }}
  {{- if eq $mode "local" }}
  volumeName: nexent-supabase-db-pv
  {{- end }}
  {{- if $storageClassName }}
  storageClassName: {{ $storageClassName | quote }}
  {{- end }}
{{- end }}
{{- end }}
