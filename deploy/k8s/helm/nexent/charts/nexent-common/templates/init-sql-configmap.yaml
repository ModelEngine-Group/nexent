apiVersion: v1
kind: ConfigMap
metadata:
  name: nexent-init-sql
  namespace: {{ .Values.global.namespace }}
  annotations:
    "helm.sh/hook-weight": "-3"
data:
  init.sql: |
{{ required "nexent-common.initSql is required" .Values.initSql | nindent 4 }}
