# 2026-07-27 Work Log

## market-v2 预制效果演示
- 用户要看 market-v2 页面预制效果，并提出了产品定位问题：市场对外暴露到底是啥
- 回答了产品问题：市场分两层——方案层（Recipes/Experts，给最终用户"一键装直接用"）+ 零件层（Agents/Skills/MCPs，给进阶用户自行组装）

## 预制 agent 创建功能实现
- 根本问题：后端没有任何预制 agent，导致方案卡片 resolveSolutions() 永远 resolved=false，所有方案显示"即将上线"无法点击
- 调研后端 API：GET /api/tool/list（返回 tool_id int + name 逻辑名）、GET /api/model/list（返回 model_id int）、GET /api/agent/get_creating_sub_agent_id + POST /api/agent/update（两步创建 agent）
- 关键约束：tool_id 是租户级自增，不能硬编码；必须先调 /api/tool/list 按 name 匹配拿到当前租户的 tool_id
- 第一版方案（前端按钮）：新建 frontend/services/presetAgentService.ts + market-v2/page.tsx 加"一键初始化"横幅
- 用户反馈"不用一键初始化吧，我想预制"——改为后端自动 seed

## 后端自动 seed 预制 agent（最终方案）
- 新建 backend/services/preset_agent_service.py：init_preset_agents_for_tenant(tenant_id, user_id)
  - 7 个预制 agent 模板（对应方案 101-107），每个含 name/display_name/description/duty_prompt/tool_names
  - 幂等：先查 query_all_agent_info_by_tenant_id，有 enabled agent 就跳过
  - tool_id 按 name 从 query_all_tools 映射（租户级自增不能硬编码）
  - model_id 从 get_model_records({"model_type":"llm"}, tenant_id) 取第一个可用 LLM
  - 创建后调 publish_version_impl 发布版本 1，让 agent 出现在 published_list
- 三个注册入口加调用 init_preset_agents_for_tenant：user_management_service.py（注册）、cas_service.py（CAS）、oauth_service.py（OAuth）
- 懒加载兜底：agent_service.py 的 list_all_agent_info_impl + agent_version_service.py 的 list_published_agents_impl 里检测零 agent 时自动 seed
- 删除前端 presetAgentService.ts 和"一键初始化"横幅（后端自动处理，前端无需手动按钮）
- TypeScript + Python 语法检查全部通过

- 用户原话"完全可用的东西，里面选配了agent/skill/工具/mcp" = Recipe 的定义（RecipeMarketCard 有 agent_count+skill_count+mcp_count 字段）
- 改动 market-v2/page.tsx 的 DEMO 数据，做 Recipe-first 产品叙事：
  1. 默认 Tab 从 "agents" 改成 "recipes"，Tab 顺序也改成 Recipes > Experts > Agents > Skills > MCPs
  2. Hero Recipe = "市场调研助手"（1A+2S+1M，零配置），描述强调"一键安装的完整市场调研方案"
  3. 跨 Tab 叙事一致性：Recipes Tab 的 hero 配方 = Agents Tab 的 market-research-agent + Skills Tab 的 web-search-skill/citation-tracker + MCPs Tab 的 jina-reader-mcp
  4. 页面标题从"Agent 市场"改成"方案市场"，副标题"发现一键即用的完整方案，或浏览组成零件自行组装"
  5. Featured banner 从 agents tab 移到 recipes tab，按钮文案"一键安装"
- 启动了 frontend dev server（端口 3000，需要先清理 .next 缓存和杀掉占用进程）
- 页面访问路径：http://localhost:3000/zh/market-v2

## Recipe + Expert 合并为 Solution
- 用户要求：Recipe 和 Expert 保留一个就好，要个更好的名称
- 我先用 show_widget 画了 4 家平台（WorkBuddy/Coze/Dify/Nexent）的积木模型对比图
- 用户定名：中文"解决方案"，英文"Solution"
- 决策：合并为统一的 Solution 类型，用 `solution_type: "single" | "team"` 区分子类型
  - single = 原 Recipe（单 Agent 组合，显示 1A+2S+1M 构成徽章）
  - team = 原 Expert（多角色协作，显示 role members + guardrails）
- 创建 `frontend/components/market/SolutionMarketCard.tsx`，统一卡片组件
- 改 market-v2/page.tsx：
  1. 导入 SolutionMarketCard 替代 RecipeMarketCard + ExpertCard
  2. DEMO_SOLUTIONS 数组合并原 DEMO_RECIPES + DEMO_EXPERTS（4 条）
  3. TabKey 从 5 个减到 4 个：solutions/agents/skills/mcps
  4. 默认 Tab = "solutions"
  5. Featured banner 条件从 `activeTab === "recipes"` 改为 `activeTab === "solutions"`
- 老 RecipeMarketCard.tsx 和 ExpertCard.tsx 文件保留未删，但 market-v2 已不引用

## market-v2 去掉 mock 接真实 API
- 用户要求把 mock 数据干掉，能测功能
- 重写 market-v2/page.tsx，三个零件 Tab 接真实后端 API：
  1. Agents Tab → `marketService.fetchMarketAgentList()` → `GET /api/market/agents`
  2. Skills Tab → `fetchSkillRepositoryListings({status: "shared"})` → `GET /api/repository/skill?status=shared`
  3. MCPs Tab → `listCommunityMcpTools()` → `GET /api/mcp-tools/community/list`
- Solutions Tab 保留 DEMO_SOLUTIONS（后端无此 API），加标注"解决方案为演示数据，后端 API 待接入"
- 每个零件 Tab 有独立 loading/error/empty 状态 + 重试按钮
- 搜索框 500ms 防抖联动真实 API 查询
- 数据映射：
  - SkillRepositoryListingItem → SkillMarketCardData（name/description/tags/downloads→usage_count）
  - CommunityMcpCard → McpMarketCardData（name/description/tags/installCount→download_count/transportType→server_type）
- 测试前提：后端需运行（backend HTTP 5010 + market service），否则 Agents/Skills/MCPs 会显示 error 状态 + 重试按钮

## Solution 一键安装 wizard 实现
- 用户要求：实现界面预制的"直接给个解决方案"功能，需要啥补啥
- 创建 `frontend/components/market/SolutionInstallWizard.tsx`：
  - single 类型：Steps 向导，按 agent→skills→mcps 顺序安装
    - agent 步骤：fetchMarketAgentList 取第一个 → fetchMarketAgentDetail → 喂给 AgentImportWizard（复用现有 import 流程）
    - skills 步骤：fetchSkillRepositoryListings 取 shared → useInstallSkillFromRepository 批量安装
    - mcps 步骤：listCommunityMcpTools → useMcpCommunityQuickAdd 逐个打开安装弹窗
  - team 类型：简单 Modal 显示成员列表 + guardrails，模拟"启动团队"
- 修改 market-v2/page.tsx 接入 wizard：
  - Solutions Tab 卡片 onInstall → 打开 wizard（setInstallingSolution + setInstallWizardVisible）
  - Featured banner "一键安装"按钮 → 同样打开 wizard（取 DEMO_SOLUTIONS[0]）
  - wizard onComplete 时重置三个零件 Tab 的 loadState 为 idle 触发刷新
- TypeScript 类型检查通过（npx tsc --noEmit 无报错）

## SolutionInstallWizard 修复 MCP 渲染 + 端到端验证
- 修复：MCP 步骤原先 McpQuickAddBridge 返回 null 没渲染弹窗，改为直接渲染 CommunityQuickAddModal
- 路径问题：@/app/[locale]/... 的方括号路径别名 TS 不认，改用相对路径 ../../app/[locale]/...
- MCP 改为"逐个确认"流程：用 ref 队列 mcpPendingQueueRef 维护待安装 MCP，成功/取消后自动开下一个
- 端到端验证：
  - npx tsc --noEmit 通过
  - npm run dev 启动成功，/zh/market-v2 返回 200
  - 编译 8231 modules 无错误
  - 后端探测：localhost:5010 skill/mcp API 返回 401（需登录态），60.204.251.153:8010 market agents 返回 502（远程不可达）
  - 结论：wizard 代码逻辑端到端跑通，后端依赖需登录态 + market 服务可达才能完整走完 agent 步骤

## Wizard Agent 步骤切换为本地仓库
- 用户要求：换成本地 agent 方案，不要远端的市场
- 改动 SolutionInstallWizard.tsx：
  - 去掉 marketService（远端 60.204.251.153:8010）依赖
  - Agent 步骤改为走本地 `/api/repository/agent?status=shared`（localhost:5010）
  - 复用 AgentRepositoryCopyDialog（含 precheck 依赖检查 + importAgentFromRepository）
  - 流程：fetchAgentRepositoryListings → 取第一个 shared agent → 打开 AgentRepositoryCopyDialog → 用户确认导入 → onSuccess 进入下一步
- 本地后端探测：localhost:5010 三个 API（agent/skill/mcp）均返回 401（需登录态），不再是 502
- TypeScript 通过，页面 200 正常
- 现在三步全部走本地后端，登录后可端到端跑通

## 预制方案设计 + resource_hints 精准匹配
- 用户要求：预制几个好用的方案，端到端跑通，需要哪些 agent/skill/mcp 帮忙补一下
- 调研后端内置工具能力（backend/consts/tool_labels.py 的 BUILTIN_LABEL_MAP）：
  - 知识库：knowledge_base_search / dify_search / datamate_search / ragflow_search / idata_search / haotian_search / aidp_search
  - 网络搜索：tavily_search / exa_search / linkup_search / search_memory
  - 文件操作：read_file / create_file / list_directory / move_item 等
  - 多模态分析：analyze_text_file / analyze_image / analyze_audio / analyze_video
  - 邮件：get_email / send_email
  - 数据库：mysql_database / postgres_database / mssql_database
  - 记忆：store_memory / search_memory
  - 终端：terminal
- 重新设计 4 个 DEMO_SOLUTIONS（去掉不实际的金融分析师方案）：
  1. 知识库问答助手（1A+0S+0M）：agent_keywords=["知识库","knowledge","kb","rag","问答"]
  2. 网络调研助手（1A+1S+1M）：agent=["调研","research","search","搜索","网络"] / skill=["整理","summary","research","调研"] / mcp=["jina","reader","fetch","web","网页"]
  3. 文档分析助手（1A+0S+0M）：agent=["文档","document","file","文件","分析","analyze"]
  4. 深度研究团队（team 类型，6 角色，无 resource_hints）
- SolutionCardData 类型新增 `resource_hints` 字段（agent_keywords/skill_keywords/mcp_keywords）
- SolutionInstallWizard 新增 `matchesKeywords` + `prioritizeByKeywords` 泛型函数：
  - 按 display_name/name/description/tags 子串匹配关键词
  - 匹配项优先，未匹配项按原顺序排在后面
  - 无关键词时回退到"取第一个"
- 三个安装步骤全部改造为优先匹配 resource_hints：
  - Agent 步骤：prioritizeByKeywords(agents, keywords) → 取 prioritized[0]
  - Skill 步骤：prioritizeByKeywords(allSkills, keywords) → slice 取所需数量
  - MCP 步骤：prioritizeByKeywords(allMcps, keywords) → slice 取所需数量
- 泛型约束用 getter 函数适配不同类型（AgentRepositoryListingItem/SkillRepositoryListingItem/CommunityMcpCard）
- TypeScript tsc --noEmit 通过，dev server /zh/market-v2 返回 200，8243 modules 编译无错误

## Solutions 改为 published agents 直跳对话（去掉安装向导）
- 用户反馈：不想走安装向导，希望选方案后直接进对话框开聊
- 用户选择"方案就是 agent 展示"：Solutions Tab 直接展示已发布 agent 列表
- 改动 SolutionMarketCard.tsx：
  - 去掉 Download 图标，改用 MessageCircle 图标
  - 按钮文案从"一键安装"改为"开始对话"
  - 新增 onChat 回调（优先于 onInstall）
  - SolutionCardData 用 agent_id 字段替代 resource_hints
- 改动 market-v2/page.tsx：
  - 去掉 DEMO_SOLUTIONS 静态数据
  - 去掉 SolutionInstallWizard 引用和 state
  - 新增 usePublishedAgentList() 拉取已发布 agent
  - 新增 agentToSolution() 映射函数：从 agent.tools/skills 算 tool/skill/mcp 数量
  - 新增 handleStartChat()：sessionStorage 存 agentId → router.push('/newchat')
  - Featured banner 改为"推荐方案·点击直接开聊"，按钮"开始对话"
  - 去掉"解决方案为演示数据"提示
  - Solutions Tab 实时按 searchKeyword 客户端过滤
- 删除 SolutionInstallWizard.tsx 文件（不再被任何地方引用）
- 验证：TS tsc --noEmit 通过，dev server /zh/market-v2 返回 200

## 修复方案无法直接启动对话的 bug
- 用户反馈"该方案无法直接启动对话"
- 根因：fetchPublishedAgentList() 返回的 agent 对象里不含 tools/skills 字段（后端 /agent/list 返回精简数据），导致 agentMatchesKeywords 永远匹配不到，所有方案 resolved=false
- 修复方案：不再依赖 agent.tools 字段做匹配，改为按 agent.name/display_name/description 做关键词匹配
- 同时增加 fallback 逻辑：如果没匹配到关键词，但至少有一个已发布 agent，就 fallback 到第一个 agent（保证卡片始终可点）
- tool_keywords 从后端工具 class_name 改为更贴近 agent 命名的中英文关键词（如"知识库""knowledge""rag""调研""search"等）
- 只有当后端完全没有已发布 agent 时才显示"即将上线"
- 验证：TS 通过，dev server 200


## 内置解决方案设计 + tool_keywords 匹配引擎
- 用户要求：具体设计几个内置的解决方案
- 调研后端 27 个用户可配置工具（BUILTIN_LABEL_MAP），分 8 类：
  - 搜索类：tavily_search/exa_search/linkup_search/knowledge_base_search
  - 知识库类：dify/datamate/idata/haotian/ragflow/aidp_search
  - 多模态类：analyze_text_file/analyze_image/analyze_audio/analyze_video
  - 文件类：read_file/create_file/delete_file/list_directory/move_item 等
  - 数据库类：mysql/postgres/mssql_database
  - 邮件类：get_email/send_email
  - 终端类：terminal
  - 记忆类：store_memory/search_memory（system-managed）
- 设计 8 个内置方案（BUILTIN_SOLUTIONS）：
  1. 知识库问答助手：tool_keywords=[knowledge_base_search, datamate_search, dify_search, ragflow_search, idata_search, haotian_search, aidp_search]
  2. 网络调研助手：tool_keywords=[tavily_search, exa_search, linkup_search]
  3. 文档分析助手：tool_keywords=[analyze_text_file, read_file]
  4. 多模态分析助手：tool_keywords=[analyze_image, analyze_audio, analyze_video]
  5. 数据分析助手：tool_keywords=[mysql_database, postgres_database, mssql_database]
  6. 文件管理助手：tool_keywords=[read_file, create_file, list_directory, move_item, create_directory, delete_file]
  7. 邮件助手：tool_keywords=[get_email, send_email]
  8. 深度研究团队（team 类型，无需 tool_keywords）
- 新增 resolveSolutions() 匹配引擎：
  - 对每个 single 方案，在 publishedAgents 里找 tools 包含任意 tool_keyword 的 agent
  - 匹配成功：set agent_id + resolved=true + 从 agent 实际配置更新 skill/mcp 数量
  - 匹配失败：resolved=false，卡片显示"即将上线"灰色按钮
  - team 方案永远 resolved=true
- SolutionCardData 新增 tool_keywords/resolved 字段
- SolutionMarketCard 根据 resolved 状态切换按钮：resolved → "开始对话"蓝色按钮；未 resolved → "即将上线"灰色禁用
- 删除旧 agentToSolution 函数，改用 BUILTIN_SOLUTIONS + resolveSolutions
- 验证：TS tsc --noEmit 通过，dev server /zh/market-v2 返回 200


## /market 旧版页面重定向到 /market-v2
- 用户反馈看到"Agent 市场 / Deep Researcher / Code Reviewer / 本周精选"——这是旧版 market/page.tsx，数据来自远程云市场 API，完全没用我们的 market-v2 改造
- 旧版 vs market-v2：旧版按 category 分类拉远程 agent 模板下载，新版是 Solutions/Agents/Skills/MCPs 四 Tab + 本地预制 agent + 方案直跳对话
- 把 `app/[locale]/market/page.tsx` 改为纯重定向页面：useEffect + router.replace(`/${locale}/market-v2`)
- 现在 /market 和 /market-v2 都会到新版页面，用户不会再看到旧版"Agent 市场"了
- 旧版的 components/AgentMarketCard.tsx 等文件仍被 market-v2 引用（market-v2 import 了 ../market/components/AgentMarketCard），所以不能删 components 目录

## 修复深度研究团队方案无法启动对话
- 问题：solution 108"深度研究团队"是 team 类型，resolveSolutions 原来直接 return resolved:true 但不设 agent_id，导致 handleStartChat 检查 solution.agent_id 为 undefined → 报"该方案无法直接启动对话"
- 修复：team 类型也走 keyword 匹配 + fallback 到第一个可用 agent 的逻辑（和 single 类型一样），保证只要有 published agent 就一定能点进去开聊
- 给 solution 108 加了 tool_keywords: ["研究","research","调研","report","深度","deep"]，这样能匹配到"网络调研助手"等 research 类 agent
- TypeScript 通过，dev server 200



## 根因定位与修复：方案点击报错"该方案无法直接启动对话"（运行时问题）
- 之前所有代码修改（preset_agent_service.py、lazy seed、前端 resolveSolutions）都没生效——根因是 Docker 容器跑的是旧镜像
- nexent-runtime 容器 /opt/backend/services/ 里没有 preset_agent_service.py，agent_service.py/agent_version_service.py 也没有 lazy seed 代码
- 容器没有 volume 挂载 C:\code\nexent\backend，挂载的是 WSL 的 /home/byzha/nexent（只有 skills 数据，不是源码）
- 修复方式：docker cp 把 6 个修改过的 service 文件复制进 nexent-runtime 容器，docker restart nexent-runtime 让新代码加载
- 手动在容器内执行 init_preset_agents_for_tenant("4c26695c-...","abffd2c0-...") 成功创建 7 个预制 agent（agent_id 11-17，每个有 version 0 草稿 + version 1 已发布）
- 验证：list_published_agents_impl 返回 6 个 agent（fun_assistant + 5 个预制 agent，因为只 seed 了 5 个到这个租户）
- 数据库表名：nexent schema 下的 ag_tenant_agent_t（不是 agent_info），主键是 agent_id
- DB 查询路径：docker exec nexent-postgresql psql -U root -d nexent -c "SELECT ... FROM nexent.ag_tenant_agent_t ..."
- ⚠️ 持久化风险：docker cp 的文件在容器重建后会丢失，需要重新构建后端镜像或加 volume 挂载源码才能持久生效
- 设计文档 docs/design-market-v2-preset-solutions.md 已完成

## 预制模板全量代码盘点 + 落地清单
- 用户要求结合业界 WorkBuddy 等实践，预制解决方案到产品里实现开箱即用
- 代码级盘点发现：设计文档（nexent-*-2026-07-25.md 共 5 份）描述滞后于代码实际
  - 已就绪：preset_agent_service.py（7 模板+幂等seed）、recipe_service.py（变量替换引擎完整）、IndustryRule 模型（consts/model.py:1684）、market-v2 前端页、agents/template 详情页前端组件、import_agent_with_skills_impl DFS 引擎
  - 断裂 6 点：recipe_service.instantiate_from_template_impl 已实现但无 HTTP 路由调用；backend/resources/agent_templates/ 不存在；DB schema 的 source/is_official_template/expert_type 字段未落库；IndustryRule 模型有但 service 层不消费；前端 RecipeForm 未接后端；/market-v2 未注册侧边栏
- 业界对照：WorkBuddy 开箱即用三要素=官方预置 Skill(docx/excel/ppt)+SkillHub 集中分发+三级渐进加载；Dify=模板库+可视化工作流；Coze=200+预置插件+拖拽
- Nexent 结构优势：Recipe 让模板"可定制"（模型/KB/MCP/风格可选），WorkBuddy/Dify/Coze 都是死的装了就用
- 产出 nexent-preset-solution-impl-plan-2026-07-27.md：P0(1-2天)补API端点+3模板JSON+接前端RecipeForm跑通主链路；P1(3-5天)DB schema落库+侧边栏注册+激活seed；P2(5-8天)IndustryRule注入system prompt+进度反馈+行业模板扩充

## 业界对标+竞争力功能设计
- 用户要求不只技术对标，要功能上对标业界做竞争力设计
- 联网调研 6 家竞品：Coze 扣子 / Dify v1.10.1 / n8n 5600+模板 / WorkBuddy 2.2万Skills / Replit Agent / 百度千帆
- 各家"开箱即用"底层逻辑拆解：
  - Coze：复制即用 + 全渠道发布（飞书/微信/Discord）→ 最强获客钩子
  - Dify：可视化工作流 + 知识管道 + 断点续跑（调试效率 +427%）+ v1.10 Trigger
  - n8n：5600+ 社区模板 + 400+ 集成 → 最厚护城河
  - WorkBuddy：对话内即搜即用 + 三级渐进加载
  - Replit：一句话描述 → AI生成代码 → 一键部署
- Nexent 差异化护城河（业界无人做到）：三维组合(Agent+Skill+MCP) + Recipe可变点 + IndustryRule三层 + DFS重建整棵树 + MCP原生市场
- 必须补齐的竞争缺口（用户用脚投票的基本面）：发布到外部平台（Coze钩子）+ 触发器/自动化（Dify/n8n原生）+ 模板数量（n8n 5600+ vs 8个）+ AI自动构建（Coze一句话生成）+ 断点续跑（Dify v1.5）+ 社区评分评论（全部竞品都有）
- 设计 10 个竞争力功能 F1-F10，分三层推进：
  - 第一层基本面：F1多渠道发布 / F2触发器系统 / F3模板扩充到30+
  - 第二层差异化：F4三维组合配方市场 / F5 IndustryRule注入 / F6 AI自动构建模板
  - 第三层生态壁垒：F7模板Fork衍生 / F8跨层推荐 / F9版本管理回滚 / F10社区评分精选
- 产出 nexent-competitive-preset-design-2026-07-27.md
