# PR 4 — Installation and Binding

Depends on PR3; flag remains disabled. Baseline correctness is mandatory; explicitly deferred advanced MCP controls are listed in the security matrix.

| Commit / one observable capability | Files expected to change | Existing Nexent services reused | Tests | Excluded work | Compatibility impact | Rollback | Completion criteria |
|---|---|---|---|---|---|---|---|
| 1. Claim durable idempotent installation operations under PostgreSQL concurrency control | operation DB model/repository/migration draft; lease service | Existing DB engine/advisory-lock pattern | Two concurrent claims; option race; owner-only renew/release; expired reclaim; completed replay; crash checkpoint | External install calls | Replaces Redis lock and workflow reservation; no Redis compatibility | Flag off; operation rows inert | At most one live owner and retry survives process loss |
| 2. Install trusted remote/package/container MCP options with baseline URL validation | installation adapters/command; option validation; existing MCP service extensions where needed | add_mcp_service, add_container_mcp_service, provenance DB | Strict declared fields; secrets absent from key/log/result; scheme/userinfo/TLS; idempotent existing record; container compensation | DNS pinning/private-IP/proxy/redirect hardening | Custom pinned transport omitted; public behavior unchanged | Compensation removes new container/record; retry operation remains failed | Explicit action creates/reuses exactly one tenant MCP |
| 3. Health-check MCP, discover/upsert Tools and persist connected checkpoint | MCP operation transitions; discovery adapter; Tool DB transaction | Existing health/discovery and Tool catalog functions | Failure at every checkpoint; retry from MCP ID; all Tool upsert; tenant scope; redacted error | Binding | Checkpoint row replaces JSON mcp workflow operation fields | Retain recoverable MCP and revert UI action while flag off | Success returns connected + discovered Tools and binds none |
| 4. Bind selected discovered MCP Tools or explicitly skip | binding command/routes; WebMcpCard; state projection | Existing ToolInstance persistence | Empty bind rejected; undiscovered/cross-tenant rejected; atomic multi-bind; idempotent; bind-vs-skip concurrency; refresh | Online completion/continuation implementation beyond response intent | JSON binding reservation removed | Transaction rollback keeps connected/unresolved | Connected item becomes resolved only by successful bind or explicit skip |
| 5. Install/recover official Skill and bind it to Draft | Skill operation adapter/command; WebSkillCard | Official ZIP installer, SkillService, SkillInstance persistence | installable/resource_missing; canonical identity; concurrent retry; blocking work off event loop; bind failure recovery; secret-free result | Arbitrary third-party Skill sources | Moves function out of catalog service | Reconcile/rollback by canonical Skill; operation remains retryable | Success means tenant Skill exists and Draft binding is enabled |
| 6. Complete online review only when all required state is resolved | online-complete command; OnlineConfigurationBar | Workflow evaluator and operation repository | Both batches required; active operation blocks; connected MCP blocks; failed/uninstalled may be explicitly abandoned; idempotent completion | Hidden continuation send lands PR5 | State shape simplified | Revert action while flag off | Completion cannot bypass unresolved connected/installing/binding state |

Minimum MCP network fallback: only HTTP/HTTPS, no embedded credentials, strict trusted options, TLS verification, no LLM-provided proxy/socket fields, tenant ownership and redacted secrets. All installation flows retain leases, idempotency, checkpoints and compensation.
