# PR 2 — Local Resources

Depends on PR1; flag remains disabled.

| Commit / one observable capability | Files expected to change | Existing Nexent services reused | Tests | Excluded work | Compatibility impact | Rollback | Completion criteria |
|---|---|---|---|---|---|---|---|
| 1. Capture a tenant-scoped immutable local Tool/Skill catalog at Session start | local catalog adapter; Session catalog schema; bounded DB query helpers | list_all_tools, Skill DB projection, Tool schema service | Real adapter queries; allowed sources; tenant isolation; legitimate empty catalog; provider failure rollback | Registry/Community/official catalogs | Inline snapshot replaces prototype Redis/shared snapshot | Flag off; revert adapter and JSON field use | Catalog is redacted, bounded, immutable and recovered from PostgreSQL |
| 2. Return stable top-five local recommendations and persist exact proof | SDK scoring module; local Tool adapter; Backend proof callback; recommendation domain | Existing Tool runtime factory | Golden multilingual scoring; stable query/batch/order/dedupe/empty; proof failure returns error | Online normalizers/search | Consolidates proof and recommendation batch | Revert Tool binding to inert builtin while flag off | Observation and stored ordered references are byte/semantically identical |
| 3. Register the local Card before actions and deliver strict redacted parameter schemas | local Card schema/component; Card verifier presentation mode; state projection | Canonical contract generator; Tool configuration schema | Forged subset/superset/reorder/empty rejected; Card action disabled before registration; secret default absent | Delivery receipt/retry finalization in PR5 | Generated payload type replaces handwritten alias | Revert Card registry entry | Only exact current proof becomes presented; declared parameters render |
| 4. Apply selected Tools/Skills atomically with strict configuration | resource command; Tool/Skill repository adapters; LocalResourcesCard | ToolInstance/SkillInstance create/update; credential path | Required/type/choice/unknown/unselected config; all-or-nothing transaction; idempotent fingerprint; concurrent requests; secret response | MCP/official installation | Removes JSON reservation/compensation used by prototype | Transaction rollback leaves batch presented and no partial binding | Persisted bindings and applied state commit together and refresh accurately |
| 5. Explicitly Skip local recommendations and continue from persisted state | skip command/route; local Card; state projection | Session transaction and existing chat response envelope | Current/stale batch; empty batch; idempotent Skip; refresh | Hidden send coordinator implemented PR5; response contract can exist now | No user-visible reduction | Revert endpoint while flag off | Skip resolves only supplied trusted batch and returns continuation intent |

PR completion requires exact proof, full Tool/Skill parity, secret invariants, tenant scoping and transactional Apply. No online catalog or installation is included.
