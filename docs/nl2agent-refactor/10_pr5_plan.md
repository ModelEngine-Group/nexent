# PR 5 — Delivery, Recovery and Release

Depends on PRs 1–4. The feature is enabled only in the final commit after all gates pass.

| Commit / one observable capability | Files expected to change | Existing Nexent services reused | Tests | Excluded work | Compatibility impact | Rollback | Completion criteria |
|---|---|---|---|---|---|---|---|
| 1. Validate complete final Cards and persist registration-aware rendered Delivery | reusable Card verifier; Delivery command; final stream integration; lifecycle coordinator | Conversation message queries and canonical schema | Latest completed assistant; history/share/remount inert; registration-before-rendered; proof/key/Draft mismatch; receipt idempotency | Failed retry in next commit | Consolidates Backend/Frontend duplicate validation paths | Flag off; no side effects visible | Refresh never redelivers successfully rendered Card |
| 2. Record canonical Card failures and auto-retry exactly twice | Delivery failure command; presentation; continuation response | Existing Conversation history and run endpoint | Four failure reasons; attempts 1/2 hidden retry; third manual; receipt API failure not counted; business state retained | General stage continuation next commit | Marker can become generated enum while preserving hidden text | Disable auto coordinator; receipts remain diagnostic | Retry count is durable and bounded with no rollback of confirmed work |
| 3. Continue hidden after every documented stage-completing action | Provider coordinator; send request; continuation types; action responses | Existing /agent/run, history index and share filtering | Requirements/model/local/online/identity/retry continue; install/bind/skip-item/failure do not; Conversation switch cancels | No new stages | Replaces scattered marker helpers | Turn off coordinator; user can manually message while flag off | Hidden messages persist normally, never render/share, and cannot cross Conversation |
| 4. Restore every interactive Card state after refresh and Conversation switch | state projection; Provider/recovery hook; Card components | Session repository and existing resource tables | Browser/service boundary recovery for models, batches, config flags, installs, discovery, binds, receipts, identity; secret value absent | None | Removes prototype sessionStorage authority and handwritten projections | Backend state remains source; revert UI hydration while flag off | Refresh at every stage yields usable equivalent controls and correct Runner |
| 5. Squash schema and regenerate final contracts | one SQL migration; Docker and K8s fresh init; DB model; local guard; exporter/generated JSON/TS | Standard checksum/advisory migration runner; FastAPI/npx generation | Migration ordering/checksum/schema parity; contracts:check; no prototype IDs/scripts | Production data backfill for unreleased feature | Deletes clean-branch prototype migration history; no released compatibility promised | Revert stack before release; migration remains unapplied while flag off | Fresh/upgrade schemas match and generated tree is clean |
| 6. Replace implementation-mock tests with boundary and end-to-end parity suite | backend integration fixtures; SDK golden tests; frontend browser tests; test runner config | Real PostgreSQL transaction fixtures, existing chat SSE harness | Full feature ledger trace; ownership/secret/finalize/concurrency/failure injection; two languages | Advanced security hardening tests move with deferred controls | Test names/fixtures may change; behavior contracts remain | No runtime effect | Every feature matrix row links to a passing test and no redundant mock-call-only test remains |
| 7. Remove prototype compatibility/temp tools and enable NL2Agent | facade aliases; Redis keys/dependency; repair script omission; old migration references; feature flag/config/docs | Existing deployment flag mechanism | Search for old keys/aliases/action Tools; smoke import; backend full suite; frontend check-all; manual E2E | Deferred security controls only | First user-visible activation | Single flag disables feature without schema/data deletion | Complete stack merged, all gates green, feature ledger and mappings verified |

Release rollback is the feature flag, not destructive schema rollback. Persisted version-0 Drafts and Sessions remain intact. No production code is enabled from an intermediate PR.
