# PR 3 — Online Catalogs and Trusted Recommendations

Depends on PR2; flag remains disabled.

| Commit / one observable capability | Files expected to change | Existing Nexent services reused | Tests | Excluded work | Compatibility impact | Rollback | Completion criteria |
|---|---|---|---|---|---|---|---|
| 1. Load Registry and Community independently with finite normal-operation bounds | marketplace adapters; Session start catalog assembly; redaction utilities | Existing Registry and Community marketplace services | Pagination termination; source failure independence; timeout/cap; legal empty; secret metadata redaction | Installation/network transports | Exact prototype 20-page/5-MiB settings may simplify while remaining finite | Flag off; remove adapters | One source failure preserves the other; malformed/unbounded provider fails safely |
| 2. Normalize Registry and Community MCPs into stable install options | MCP catalog domain/adapter; shared normalized DTO schema | Existing marketplace payload clients | Golden remote/package/container/header/env/template fixtures; Registry precedence; stable IDs; unsupported option | Side effects and URL connection | Moves normalization from SDK to Backend without payload change | Revert normalized snapshot version | Current Card payloads and install option semantics are preserved |
| 3. Load official Skill Catalog with installable/resource_missing/installed semantics | official Skill adapter; Session catalog | get_official_skills_with_status and official ZIP metadata | resource_missing retained online/excluded local; installed not recommended; item parse failure isolation | Actual install/recovery | Removes prototype filtering discrepancy | Revert adapter while flag off | Searchable official snapshot contains every recoverable candidate |
| 4. Search online MCP and Skill catalogs with shared scoring and exact proof | two SDK Tool adapters; shared scoring; Backend proof callback | Per-run Tool factory | Top-five/order/dedupe/query language; proof persistence failure; no SDK network/Redis/global cache | Install endpoints | Thin Tools replace large SDK normalization wrapper | Disable Tools in Runner while flag off | Both searches produce canonical schema-valid Cards from immutable snapshot |
| 5. Register strict MCP and Skill Cards and persist trusted presentation | canonical Card defs; verifier; WebMcpCard/WebSkillCard read-only portions | Contract generator and Provider lifecycle | Fake/reordered/duplicate batches; singular/plural tags; Conversation/Draft mismatch; refresh | Install buttons remain disabled until PR4 | Generated types replace webMcpTypes/cardPayloadTypes | Revert registry entries | Exact batch is actionable only after registration; all current online results display |

PR completion requires Registry, Community and official capability parity, trusted proof, normalized options, bounded loading and no side effect from search.
