# PR 1 — Core Workflow and Draft Builder

Feature flag remains disabled. This PR establishes identities and the PostgreSQL aggregate; it does not expose a partial Builder.

| Commit / one observable capability | Files expected to change | Existing Nexent services reused | Tests | Excluded work | Compatibility impact | Rollback | Completion criteria |
|---|---|---|---|---|---|---|---|
| 1. Provision exactly one tenant Runner with three read-only search Tools | default Runner config; tool_db; narrow provisioner; config startup; flag config | Agent creation, Prompt template sync, ToolInfo/ToolInstance | Provision twice; wrong/missing model/Prompt/tool repair; tenant uniqueness | Catalog search implementations and all actions | Adds hidden internal Agent only; old prototype entry stays disabled | Disable flag and remove provision call; idempotent rows can remain hidden | Two concurrent provisions converge; exactly three builtin Tools |
| 2. Start one durable owned Session containing distinct Runner, Draft and Conversation | Session model/repository/service; Agent/Conversation caller-owned transaction hooks; start/list/recover/abandon routes | Agent create, Conversation create/list/delete, auth context | Real transaction rollback; unequal IDs; owner/tenant isolation; by-Conversation recovery; terminal rejection | Resource catalogs beyond empty typed slots; Redis | New strict Session API behind flag | Revert routes/schema commit while flag off | No orphan on injected failure; recovery returns persisted Runner/Draft/Conversation |
| 3. Evaluate and persist requirements collection/registration/explicit confirmation | workflow domain/evaluator; requirements commands; Agent run context; bilingual prompts; summary Card | Existing chat history and agent execution | Pure stage vectors; five-field normalization/fingerprint; text confirmation rejected; stale Card; revision CAS | Models/resources/install/Delivery retry | Replaces prototype v2 state with clean release schema; no migration compatibility needed | Flag off; database migration reverted before release | All requirement paths survive refresh and only Card button confirms |
| 4. Select primary and ordered fallback models atomically | model request/route/command; Draft repository adapter; ModelSelectionCard | Model availability APIs and Agent model fields | Tenant/type/availability/order/max-five/output-token; concurrent retry; persisted UI recovery | Local/online resources | Strict request may reject prototype coercions intentionally | Revert commit; existing Draft fields remain valid | Agent fields and Session step commit together; Finalize precondition sees persisted selection |
| 5. Save display name, render persisted final review and Finalize version-0 draft_ready | identity/finalize commands; final projection; AgentIdentityCard; FinalizeCard | Agent update, model/resource validation/query services | Name length/collision; proposal allowlist; dangling/cross-tenant refs; one-transaction Draft+completed Session; no publish | Actual resource search/install; full delivery/continuation | Removes model/resource IDs from Finalize payload; intentional strict contract | Flag off; completed test Drafts disposable | Finalize uses persisted identity/models/resources, returns draft_ready, and normal list still hides internals |
| 6. Establish core generated contracts and Provider skeleton | canonical Card schema core defs; FastAPI models; exporter; generated TS/schema; Provider/API client | FastAPI OpenAPI and frontend chat selection | Contract regeneration diff; core Cards under Ajv; Session switch identity | Resource Card definitions may be present but inert; automatic continuation deferred to PR5 | Generated types replace handwritten core DTOs | Regenerate previous sources | contracts:check clean and Provider cannot cross Session scope |

PR completion requires the feature flag off, no Redis in the PR1 path, strict owner/identity validation, and reviewable transactions. Rollback never exposes a partially built Draft because the flag remains disabled.
