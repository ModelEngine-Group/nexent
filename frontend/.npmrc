# Hoist AG-UI and assistant-ui packages to top-level node_modules so that
# standalone `tsc --noEmit` can resolve them (pnpm's default isolated
# node_modules structure hides them from direct tsc invocations).
shamefully-hoist=true
